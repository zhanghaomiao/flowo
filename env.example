# Application Settings
DOMAIN=localhost
# External port for Caddy reverse proxy (default: 80)
# Note: Caddy listens on port 80 internally, this maps to external port
# Change this if port 80 is occupied, e.g. PORT=3100
# The service will be accessible at http://localhost:${PORT}
PORT=80
TZ = Asia/Shanghai

# Database Configuration
POSTGRES_DB=flowo_logs
POSTGRES_USER=flowo
POSTGRES_PASSWORD=flowo_password
POSTGRES_HOST=localhost
# External port for PostgreSQL (default: 5432)
# Note: This is the external port mapping. Container-to-container communication uses port 5432 internally.
# Change this if port 5432 is occupied, e.g. POSTGRES_PORT=5555
POSTGRES_PORT=5432


# Workflow Directory
FLOWO_WORKING_PATH=/path/to/your/workflowdir

# Frontend Development Settings
# VITE_DISABLE_SSE=true
# Custom API base path for frontend (optional)
# - Leave empty/unset for production: frontend uses same origin (Caddy handles routing)
# - For development: set to backend URL, e.g. VITE_API_BASE_PATH=http://localhost:3100
# Examples:
# VITE_API_BASE_PATH=http://localhost:3100
# VITE_API_BASE_PATH=https://api.example.com
# If not set, defaults to current origin (recommended for production)
